<main class="error-page">
  <h1><%= title %></h1>
  <p><%= message %></p>

  <% if (statusCode === 404) { %>
    <p>Sorry, we appear to have lost that page.</p>
  <% } else if (statusCode === 500) { %>
    <p>Oh no! There was a crash. Maybe try a different route? .</p>
  <% } %>

  <a href="/" class="btn">Return to Home</a>
</main>
